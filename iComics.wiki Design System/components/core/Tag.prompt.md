Outlined pill for genres / keywords / filters.

```jsx
<Tag>Romance</Tag>
<Tag interactive pressed onClick={toggle}>Fantasy</Tag>
```

Static `span` by default; `interactive` makes it a toggle `button` (use with `pressed`). For status/source use `Badge`.
