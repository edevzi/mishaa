Underline tab bar for switching catalog sources/categories.

```jsx
<Tabs value={tab} onChange={setTab} tabs={["Manga Hub", "Manhwa", "Webtoons", "Trending", "New"]} />
```

Scrolls horizontally on overflow. For mutually-exclusive compact modes use `SegmentedControl` instead.
